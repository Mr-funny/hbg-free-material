#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
project_root=$(CDPATH= cd -- "$script_dir/.." && pwd)

cd "$project_root"
mkdir -p "${HBG_MATERIAL_HOST_OUTPUT_DIR:-./downloads}"
exec docker compose --profile cli run --rm cli "$@"
